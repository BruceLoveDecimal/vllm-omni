# Audio8 TTS Preview 0.1B deploy: async-chunk streaming hybrid_slow_ar -> codec_decoder.
# Model: Audio8/Audio8-TTS-Preview-0.1b (model_type = "arktts", slow_backbone = "falcon_h1").
#
# Same topology as audio8_tts.yaml; stage 1 (codec) is identical. Stage 0 runs
# the Falcon-H1 hybrid Slow AR, so attention KV is paged while the Mamba2 SSM
# state occupies fixed per-request slots.
#
# trust_remote_code is deliberately OFF: the checkpoint ships an `auto_map`, and
# transformers prefers remote code over registered classes when it is enabled,
# which would bypass vllm-omni's Audio8TTSConfig. The checkpoint's own remote
# code also pins transformers 4.57 (it imports a cache class later releases
# removed), so enabling it would constrain the whole server.
async_chunk: true
enable_chunked_prefill: false

connectors:
  connector_of_shared_memory:
    name: SharedMemoryConnector
    extra:
      codec_streaming: true
      connector_get_sleep_s: 0.01
      connector_get_max_wait_first_chunk: 3000
      connector_get_max_wait: 300
      # ~21.5 Hz codec; 25 frames ~= 1.16 s of audio.
      codec_chunk_frames: 25
      codec_left_context_frames: 25
      initial_codec_chunk_frames: 4
      # float32 keeps the Snake activations / weight-normed convs clean.
      audio8_tts_codec_dtype: float32

stages:
  - stage_id: 0
    # >= 4 so concurrent requests pipeline; a default of 1 starves the codec
    # window and produces audible gaps under load. This also exercises the
    # per-request Mamba state slots, which a single sequence never would.
    max_num_seqs: 4
    # 0.1B weights are ~340 MB; the hybrid cache adds a small fixed SSM slot per
    # sequence on top of the paged attention KV.
    gpu_memory_utilization: 0.2
    enforce_eager: false
    trust_remote_code: false
    # Mamba state is not prefix-cacheable the way attention KV is; leave off.
    enable_prefix_caching: false
    async_scheduling: false
    # The checkpoint's max_seq_len is 2048 packed text+audio positions; with
    # chunked prefill off, max_num_batched_tokens must be >= max_model_len.
    max_model_len: 2048
    max_num_batched_tokens: 2048
    devices: "0"
    output_connectors:
      to_stage_1: connector_of_shared_memory
    default_sampling_params:
      # generation_config.json of the released checkpoint.
      temperature: 0.7
      top_k: 50
      top_p: 0.9
      max_tokens: 1024
      seed: 42
      repetition_penalty: 1.0

  - stage_id: 1
    max_num_seqs: 1
    # The codec is ~1.4 GB of weights plus per-chunk activations.
    gpu_memory_utilization: 0.1
    enforce_eager: true
    trust_remote_code: false
    enable_prefix_caching: false
    async_scheduling: false
    max_model_len: 2048
    max_num_batched_tokens: 2048
    devices: "0"
    input_connectors:
      from_stage_0: connector_of_shared_memory
    default_sampling_params:
      temperature: 0.0
      top_p: 1.0
      top_k: -1
      max_tokens: 65536
      seed: 42
      repetition_penalty: 1.0
